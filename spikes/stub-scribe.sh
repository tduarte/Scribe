#!/bin/sh
echo "Scribe spike build -- the app itself is not built yet."
echo "Run the Phase 0 spikes:"
echo "  flatpak run --command=spike-shortcuts io.github.tduarte.Scribe"
echo "  flatpak run --command=spike-inject    io.github.tduarte.Scribe [paste|type|both]"
